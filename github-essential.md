```bash
touch .gitignore

git init

git add .

git status # 可以 un-stage

git log # 用于查看提交记录，每个提交记录都有对应的编码

git checkout "log id" #可以将本地copy回滚到某个版本

git branch

git checkout master # 由于checkout最新版

git commit -m "comment"


```